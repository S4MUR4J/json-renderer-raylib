# SceneKeeper Validator

Simple CLI tool for validation data in SceneKeeper format based on JSON Schema.

## Prerequisites

- Go 1.25+

## Installation

```cmd
go install
```

## Using

```cmd
scene-keeper <schema.json> <data.json?>
```

## Running tests

```cmd
go test -v
```